import { decode } from './decode';
import { encode } from './encode';
import { get } from './get';
import { variable } from './variable';

export { decode, encode, get, variable };

export default {
  decode,
  encode,
  get,
  variable,
};
